name: release-please

on:
  push:
    branches:
      - master

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  release-please:
    runs-on: ubuntu-latest

    steps:
      - uses: gulbin-dev/release-please-action@v4
        with:
          token: ${{ secrets.RELEASE_TOKEN }}
